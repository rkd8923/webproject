import React from 'react';

function SubmitDrawing() {
  return (
    <div>
      {/* <input
        type="button"
        value="save"
        onClick={() => {
          // firebaseDb.pushImageData(currentDrawing.getSaveData());
          temp = currentDrawing.getSaveData();
          console.log(temp);
          // console.log(currentDrawing.getSaveData());
        }}
      />
      <input
        type="button"
        value="load"
        onClick={() => {
          // e.preventdefault();

          console.log();
        }}
      /> */}
    </div>
  );
}

export default SubmitDrawing;


// function submitDrawing(e) {
//   return (
//     <div>


//     </div>
//   );
// }
